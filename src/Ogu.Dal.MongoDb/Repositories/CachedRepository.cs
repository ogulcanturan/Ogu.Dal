namespace Ogu.Dal.MongoDb.Repositories
{
    //Todo
    //public class CachedRepository<TEntity, TId> : Repository<TEntity, TId>, IRepository<TEntity, TId> where TEntity : class, IBaseEntity<TId>, new()
    //{
    //    public CachedRepository(IMongoClient client, string database = null, string table = null) : base(client, database, table)
    //    {
    //    }
    //}
}
